# action.yml
name: 'Profanity check'
description: 'Check profanity'
inputs:
  comment:
    description: 'body of the comment'
    required: true
    default: 'something'
outputs:
  profanity:
    description: 'profanity'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.comment }}